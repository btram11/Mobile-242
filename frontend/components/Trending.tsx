//@ts-nocheck

import { FlatList, Text, View } from "react-native";

function Trending() {
    return (
            // <FlatList
            //     data={[{ id: 1 }, { id: 2 }, { id: 3 }]}
            //     keyExtractor={(item) => item.id}
            //     renderItem={({ item }) => (
            //         <Text>{item.id}</Text>
            //     )}
            //     horizontal
            // />
            <View></View>

    )
}

export default Trending;